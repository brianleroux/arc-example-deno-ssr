{
  "scripts": {
    "build": "mkdir -p dist && deno bundle src/browser.tsx > dist/browser.js",
    "start": "npm run build && npx sandbox"
  },
  "dependencies": {
    "@architect/sandbox": "^1.9.6"
  }
}
